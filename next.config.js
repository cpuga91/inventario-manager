/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {},
  allowedDevOrigins: [
    "*.replit.dev",
    "*.spock.replit.dev",
    "127.0.0.1",
  ],
};

module.exports = nextConfig;
